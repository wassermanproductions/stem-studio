# Audio I/O and DSP (used by the CLI, stub engine, pipeline, and eval harness).
numpy>=1.24
scipy>=1.10
soundfile>=0.12

# TIGER-DnR ML engine. torch is a large (~2 GB) install; first-run setup can
# take a few minutes. torchaudio is included for parity with the model stack.
torch>=2.2
torchaudio>=2.2

# Model download from the Hugging Face Hub + safetensors weight loading.
huggingface_hub>=0.24
safetensors>=0.4
