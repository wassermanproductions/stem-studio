# Runtime bootstrap assets

`npm run prepare:win-runtime` populates `windows/` on a native Windows x64 host
with the pinned uv bootstrapper plus the approved BtbN GPL FFmpeg/FFprobe pair,
licenses, hashes, and provenance. Generated release inputs are not committed.
