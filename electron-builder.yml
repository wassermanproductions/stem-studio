appId: com.wassermanproductions.stemstudio
productName: Stem Studio
directories:
  output: release
files:
  - out/**
  - package.json
# The Python worker ships as source; the app builds its own venv at runtime
# under userData. Bundle the worker + requirements so first-run setup works.
extraResources:
  - from: python
    to: python
asarUnpack:
  - '**/*.py'
mac:
  category: public.app-category.music
  icon: build/icon.icns
  target:
    - target: dmg
      arch: [arm64, x64]
  # Unsigned by default. For distribution beyond your own machines, set
  # identity + notarize (Apple Developer ID) — see README "Sharing builds".
  identity: null
win:
  target: nsis
linux:
  # Built for the NVIDIA DGX Spark (DGX OS / Ubuntu, arm64). AppImage runs
  # anywhere; the .deb installs on Debian/Ubuntu-based distros.
  icon: build/icon.png
  category: AudioVideo
  target:
    - target: AppImage
      arch: [arm64]
    - target: deb
      arch: [arm64]
npmRebuild: false
